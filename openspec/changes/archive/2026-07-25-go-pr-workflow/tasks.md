## Tasks

- [x] Create the reusable Go pull request workflow template.
- [x] Wire format verification, lint, and tidy as parallel jobs.
- [x] Add the serialized build job.
- [x] Wire test, test-race, and test-cover as parallel jobs after build.
- [x] Document the non-mutating format verification requirement.
- [x] Add an example consumer configuration if needed.
